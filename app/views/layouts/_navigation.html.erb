<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Coin_sms_service', main_app.home_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
     <ul class="nav navbar-nav navbar-right">
                      <% if user_signed_in? %>
                        <li class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_user.email%> <span class="caret"></span></a>
                          <ul class="dropdown-menu" role="menu">
                            <li><%= link_to "Edit your account", main_app.edit_user_registration_path %></li>
                           <%#- if current_user.admin? %>
				 <li> <%#= link_to "Admin",main_app.admin_root_path  %> </li>
				<%#- end %>
                            <li class="divider"></li>
                            <li><%= link_to "Sign out", main_app.destroy_user_session_path, data:{method: :delete} %></li>
                          </ul>
                        </li>
                      <% end %>
   
                      </ul>
    
    </div>
  </div>
</nav>
